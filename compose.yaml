services:
  site:
    image: ruby:3.3
    working_dir: /site
    command: bash -lc "bundle install && bundle exec jekyll serve --host 0.0.0.0 --port 4000 --force_polling"
    ports:
      - "4000:4000"
    volumes:
      - .:/site
      - bundle:/usr/local/bundle

volumes:
  bundle:
